// Configuração
var testObj = {
  "hat": "ballcap",
  "shirt": "jersey",
  "shoes": "cleats"
};

// Altere apenas o código abaixo desta linha

var hatValue = testObj.hat;      // Altere esta linha
var shirtValue = testObj.shirt;    // Altere esta linha