// Configuração
var testObj = {
  "an entree": "hamburger",
  "my side": "veggies",
  "the drink": "water"
};

// Altere apenas o código abaixo desta linha

var entreeValue = testObj["an entree"];   // Altere esta linha
var drinkValue = testObj["the drink"];    // Altere esta linha